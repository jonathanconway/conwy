# components/mdx

Mdx components are used by `@mdx-js` to render Markdown elements in `.md` files as React components.

Mdx components for supported Markdown elements are looked up in `src/mdx-components.tsx`.
