import { style } from "@vanilla-extract/css";

export const sectionsList = style({
  display: "flex",
  flexDirection: "column",
  gap: "1.5rem",
  marginTop: "1.5rem",
});

export const studiesList = style({
  display: "flex",
  flexDirection: "column",
  gap: "1rem",
});
