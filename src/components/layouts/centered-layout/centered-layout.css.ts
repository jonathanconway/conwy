import { style } from "@vanilla-extract/css";

export const centeredLayoutContainer = style({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  flexDirection: "column",
  flex: 1,
});
