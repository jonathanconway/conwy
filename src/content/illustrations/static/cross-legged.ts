import { createIllustrationStatic } from "@/framework/client";

export const crossLeggedIllustrationStatic = createIllustrationStatic({
  slug: "cross-legged",
  primaryColor: "rgb(228,156,124)",
});
