import { createIllustrationStatic } from "@/framework/client";

export const codeHtmlColorIllustrationStatic = createIllustrationStatic({
  slug: "code-html-color",
  primaryColor: "rgb(124,193,252)",
});
