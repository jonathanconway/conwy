import { createIllustrationStatic } from "@/framework/client";

export const zoomDynamicColorIllustrationStatic = createIllustrationStatic({
  slug: "zoom-dynamic-color",
  primaryColor: "rgb(116,116,220)",
});
