import { createIllustrationStatic } from "@/framework/client";

export const sphereDynamicColorIllustrationStatic = createIllustrationStatic({
  slug: "sphere-dynamic-color",
  primaryColor: "rgb(235,176,139)",
});
