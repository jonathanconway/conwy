import { createIllustrationStatic } from "@/framework/client";

export const tickDynamicColorIllustrationStatic = createIllustrationStatic({
  slug: "tick-dynamic-color",
  primaryColor: "rgb(141,212,180)",
});
