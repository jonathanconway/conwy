import { createIllustrationStatic } from "@/framework/client";

export const cubeDynamicPremiumIllustrationStatic = createIllustrationStatic({
  slug: "cube-dynamic-premium",
  primaryColor: "rgb(250,238,140)",
});
