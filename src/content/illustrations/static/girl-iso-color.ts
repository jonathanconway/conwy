import { createIllustrationStatic } from "@/framework/client";

export const girlIsoColorIllustrationStatic = createIllustrationStatic({
  slug: "girl-iso-color",
  primaryColor: "rgb(228,170,129)",
});
