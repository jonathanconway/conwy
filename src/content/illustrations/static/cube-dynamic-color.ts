import { createIllustrationStatic } from "@/framework/client";

export const cubeDynamicColorIllustrationStatic = createIllustrationStatic({
  slug: "cube-dynamic-color",
  primaryColor: "rgb(203,228,194)",
});
