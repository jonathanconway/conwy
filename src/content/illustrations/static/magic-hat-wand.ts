import { createIllustrationStatic } from "@/framework/client";

export const magicHatWandIllustrationStatic = createIllustrationStatic({
  slug: "magic-hat-wand",
  primaryColor: "rgb(151, 23, 36)",
});
