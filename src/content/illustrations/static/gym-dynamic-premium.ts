import { createIllustrationStatic } from "@/framework/client";

export const gymDynamicPremiumIllustrationStatic = createIllustrationStatic({
  slug: "gym-dynamic-premium",
  primaryColor: "rgb(188,168,100)",
});
