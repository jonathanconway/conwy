import { createIllustrationStatic } from "@/framework/client";

export const folderDynamicColorIllustrationStatic = createIllustrationStatic({
  slug: "folder-dynamic-color",
  primaryColor: "rgb(228,204,152)",
});
