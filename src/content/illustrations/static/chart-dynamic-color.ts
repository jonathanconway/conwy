import { createIllustrationStatic } from "@/framework/client";

export const chartDynamicColorIllustrationStatic = createIllustrationStatic({
  slug: "chart-dynamic-color",
  primaryColor: "rgb(180,156,220)",
});
