import { createIllustrationStatic } from "@/framework/client";

export const mapPinDynamicColorIllustrationStatic = createIllustrationStatic({
  slug: "map-pin-dynamic-color",
  primaryColor: "rgb(224,180,169)",
});
