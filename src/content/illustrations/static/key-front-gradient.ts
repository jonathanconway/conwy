import {
  IllustrationStatic,
  createIllustrationStatic,
} from "@/framework/client";

export const keyFrontGradientIllustrationStatic = createIllustrationStatic({
  slug: "key-front-gradient",
  primaryColor: "rgb(228,163,114)",
});
