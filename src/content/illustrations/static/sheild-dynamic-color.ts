import { createIllustrationStatic } from "@/framework/client";

export const sheildDynamicColorIllustrationStatic = createIllustrationStatic({
  slug: "sheild-dynamic-color",
  primaryColor: "rgb(160,211,176)",
});
