import { createIllustrationStatic } from "@/framework/client";

export const sunDynamicColorIllustrationStatic = createIllustrationStatic({
  slug: "sun-dynamic-color",
  primaryColor: "rgb(236,192,129)",
});
