import { createIllustrationStatic } from "@/framework/client";

export const pullRequestIllustrationStatic = createIllustrationStatic({
  slug: "pull-request",
  primaryColor: "rgb(151,180,221)",
});
