import { meta } from "./meta";

- Rebuilt a TV talent management system (using HTML5, ASP.NET MVC 3 and SQL Azure).
- Improved database performance by refactoring LINQ code to reduce lookups, creating indexes on tables and moving calculation logic into views.
- Built a custom Javascript framework to enable partial page loading, while supporting the back-button.
- Fixed bugs on the back-end API for the iPhone app (PHP).
- Ensured cross-browser compatibility (including IE 7, 8 and 9).
- Wrote custom Javascript framework to partially load pages while supporting browser back-button/history.