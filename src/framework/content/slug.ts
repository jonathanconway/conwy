/**
 * `Slug` is a part of a URL that identifies a specific piece of `Content` in a user-friendly way.
 * It helps both users and search engines understand the content.
 * It consists of descriptive keywords in all lower case separated by hyphens.
 */
export type Slug = string;
