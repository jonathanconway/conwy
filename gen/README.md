# `gens` folder

Code generators used at development time to quickly scaffold code and content structures.

## Usage

```
npm run gen {generator} -- [--{param1}="{value1}" ...]
```

- `{generator}` - name of the generator in camelCase
- `{param1}` - name of parameter to pass
- `{value1}` - value to pass for parameter
